const Loading = () => {
  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <div>Yükleniyor...</div>
    </div>
  );
};

export default Loading;
